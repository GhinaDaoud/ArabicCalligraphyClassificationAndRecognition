Overview:

1) hiratehseen 
-- reads the images in grayscale, resizes them to 105x105, defines 4 manual augmentations (noise, blur, affine adn gradient)
-- starts from 466 and expands them to 7456 samples through comninatorial augmentation, then saves the augmented images in a folder
-- it maps 5 classes: Diwani, Kufi, Maghribi, Mohakek, Parsi; split 75/25 for train-test
-- Local Phase-Quantization LPQ feature calculation of length 256, then feeds that 1 feature vector into a heavy 1D CNN + huge dense stack => 45.45M parameters for only 5 classes
--> design is heavily feature-engineered and likely over-parameterized for the underlying task, though it reaches high accuracy quickly  

2) mhmoodlan
-- it wraps a FontModel around RuFa dataset and a CNN network 
-- images at 100x100 resolution with 516 real images from RuFa (Ruqaa, Nastaaliq)
-- a model of 3-layer CNN, followed by a 20layer fully connected network with dropout, Adam and binary cross-entropy loss
-- CNN: Conv2D(16), MaxPooling, Dropout, Conv2D(32), MaxPooling, Dropout, Conv2D(64), MaxPooling, Dropout, 
-- Flatten, Dense(128), Dropout, Dense(16), Dropout, Dense(1) for binary classification
--> ~97% accuracy  

3) LoayMo99
-- 9 classes: Diwani, Kufi, Maghribi, Mohakek, Parsi, Rekaa, Naskh, Thuluth and Squar-kufic
-- simple CNN classification and feature-based classification with SVM, KNN, Voting and Random Forest

4) melhazzouri
-- trains and tests on RuFa with 100x100 RGB inputs
-- small CNN: rescalong, Conv2d(32), MaxPooling, Conv2D(16), MaxPooling, Dropout(0.5), Flatten, Dense(64) with softmax
-- trained for 8 epochs with Adam and categorical cross-entropy loss
-- computes confusion matrix, accuracy, precision, classification report 
-- training on synthetic data and testing on real data 

6) Mustapha-AJEGHRIR
-- trained on RuFa dataset
-- CNN: nut Conv2D(16), MaxPooling, DropOut, Conv2D(32), MaxPooling, Dropout, Conv2d(64), MaxPooling, Dropout, 
-- Flatten, Dense(128) DropOut, Dense(16), DropOut, Dense(1) for binary classification  
-- trained for 10 epochs, `Adam` optimizer, binary cross-entropy loss
-- ~96% accuracy 

why using LPQ features?
it is used as a threshold-based preprocessing
based on quatized local Fourier phase information 
the key signal lies in local phase/ local texture/ blur-robust stroke patterns and not high-level object shape 
which is a good intuition for calligraphy and font style 

LPQ computes local phase-based texture descriptors and is known for blur-intensive texture classification
it helps in arabic calligraphy because still differences lie in stroke thickness, local curvature, edge endings, 
slant, junction shape and texture-like repeated pen patterns 
because the task isnt a generic image classification but rather a fine-grained stroke-style classification
--> we need to:
    1- capture local stroke texture 
    2- capture larger style layout/spatial rhythm
    3- avoid depending on handcrafted features like LPQ
network should learn what LPQ was trying to give, but natively 
because once we extract LPQ features, the model is limited to the information contained in those features, 
and cannot learn from raw pixel data to discover other potentially useful features


then..
choose a backbone for feature extraction that can capture local texture patterns and spatial relationships
---> ResNet18 or ResNet34, DenseNet, EfficientNet, ViT, Swin, ConvNeXt

fine-grained classification is hard, because classes differ by subtle local details, not big oject-level differences
recent fine-grained work emphasizes the need to preserve and combine local detailed cues with global semantic information:
https://www.frontiersin.org/journals/neurorobotics/articles/10.3389/fnbot.2023.1301192/full?utm_source=chatgpt.com
https://www.nature.com/articles/s41598-022-23835-0?utm_source=chatgpt.com
--> glabal style flow and local stroke details matter (two-feature stream architecture)
a tiny CNN may learn something, but it ca easily become weak or too coarse

Image
  ↓
ResNet backbone (shared)
  ├── Mid-level features  → local/detail descriptor (early layers)
  └── Final features      → global/semantic descriptor (deep layers)
Concatenate
  ↓
MLP classifier
  ↓
Softmax

Global branch helps with:
- visual rhythm / spacing / overall layout / slant tendency / letter proportion style 
Local branch helps with:
- stroke endings / curvature / pen width changes / junction patterns / local texture-like stroke behavior 


HOW?
1) input
- image resized to 224x224 in grayscale or 3-channels cause ResNet expects 3 channels 
- if images are grayscale, we can duplicate the single channel to create a 3-channel input for ResNet

2) backbone
- start with ResNet18 pretrained 
- we need two stages of features: 
    - mid-level features from an early layer (e.g. after layer3 or layer2) for local details
    - final features from the last layer before classification for global style like layer4

3) local branch 
- take feature map from layer2 or layer3; it still keeps meaningful spatial detail while already being more abstract than raw edges; which preserves smaller stroke cues better 
- then pass it rhrough something simple like a 1x1 convolution to reduce channels -> BacthNorm -> ReLU -> Global Average Pooling 
--> vector 1 ∈ R^d; a local-detail descriptor 

4) global branch
- take the final feature vector from layer4 
- then apply Global Average Pooling to get vector g ∈ R^d; a global-style descriptor with depth and semantic 

5) Fusion 
- concatenate local and global vectors: z = [l; g] ∈ R^(2d)
- then use a small classifier head: Linear(512) or Linear(256) -> ReLU -> Dropout(0.3) -> Linear(num_classes) -> Softmax

6) Training
- use cross-entropy loss for multi-class classification
- data augmentation: random rotations, scaling, horizontal flips, brightness/contrast adjustments to increase robustness to variations in calligraphy style and image quality
- train for 20-30 epochs with early stopping based on validation loss to prevent overfitting
- use AdamW optimizer 
- batch size of 8 or 12 or 16 depending on GPU memory
- weight decay of 1e-4 to regularize the model and prevent overfitting
- Use a cosine decay scheduler, ideally with a short warmup like 3-5 epochs to stabilize training
- backbone needs a small LR like 1e-4; new head/ local-global fusion layers need larger LR like 5e-4 or 1e-3

--- Workflow ---
Input image
   ↓
ResNet18 backbone
   ├── layer3 output → local branch → pooling → l
   └── layer4 output → global branch → pooling → g
Concatenate [l ; g]
   ↓
MLP classifier head
   ↓
Softmax over style classes

Data:
augmentation:  augment.py <path_to_dataset_root>
output folder structure:
out
├── images
│   └── [.png files]
├── labels.csv
└── analytics.png

Input size difference:
- best choice when working with ResNet is to have consistent input size 
- so.. need to standardize the image resolutions before feeding into the model
--> forcce a specific aspect ration of ~224x224
    - pad small images to 224x224 with black borders (if they are smaller)
    - for larger images, resize them to 224x224 while maintaining aspect ratio, and then pad the remaining area to reach 224x224
--> fixed-size tensor input for batch processing 
but.. if most images have a triangular format; then rescale to a rectangle not a square shape 
like 256x128 or 384x192; which is more natural for the calligraphy images and may preserve more details without excessive padding


Data augmentation (done by Wissam):
1- mixed labels are removed from csv file 
2- augmentation counts are keyed by basename -> check duplicates and image renaming to avoid overwriting
3- balancing dataset: unlabeld-image filtering uses a hardcoded 80% threshold of he largest labeled class;
   while synthetic augmentation uses a target_ratio of 60%
4- augmentation: ElasticTransform and wide rotation range are strong for a classification work 
   because they might change the stroke geometry instead of just adding realistic variations
5- random seed is none -> fix it for data generalization and reproducibility

how it works?
if labels.csv file exist in data folder, it uses it as GT; else it uses the folder name as a label for font style 
it keeps labeled images, then selectively keeps some unlabled images for underrepresented classes to minority classes and light random augmentation to majority classes
weites everything into augmented/images and augmented/labels.csv with naming convention: CLASS_000000.png


New dataset:
- won't use Misc dataset; AC_data is richer and has duplicated with Misc; more classes
- RuFa Ruq'ah and Nasta'liq 
- DuwaBench all classes
- HICMA all classes 

Classes distribution before augmentation:
  Diwani: 657
  Kufic: 292
  Muhaqaq: 207
  Naskh: 4025
  Nasta'liq: 500
  Ruq'ah: 520
  Thuluth: 1911

Naskh is exremely dominant and Thuluth is also large 
-> reduce imbalance w/o creating a synthetic-heavy dataset
  -> target around 40-60% of the largest class or around 1000-1500 samples per class after augmentation
    -> look also for wrighted sampling 

Diwani, Ruq'ah, Nasta'liq: up to 1x extra
Kufic: up to 2x extra
Muhaqaq: up to 3x extra

The actual image transforms are intentionally conservative and fast:
    small rotation
    small translation via affine transform
    mild contrast change
    mild brightness change
    occasional light blur
    occasional sharpening




--- First training ---
(pytorch_course) C:\Users\user\OneDrive - American University of Beirut\Courses\NeuralNetworks-EECE693\Project\ArabicCalligraphyClassificationAndRecognition>python src\training\train.py --config configs\resnet18_local_global.yaml
Training setup
  Device: cuda
  Model: resnet18_local_global
  Num classes: 7
  Train samples: 8773
  Val samples: 2193
  Train batches: 1097
  Val batches: 275
  Batch size: 8
  Epochs: stage1=8, stage2=15
  Optimizer: AdamW | backbone_lr=0.0001 | head_lr=0.0005 | wd=0.0001
  Scheduler: cosine | t_max=33

[Epoch 001/023] stage=warmup | lr_backbone=9.95343e-05 | lr_head=0.000497671
  train: loss=0.7030 | acc=0.7601 | f1=0.7016 | precision=0.7217 | recall=0.6884
  val:   loss=0.3172 | acc=0.8969 | f1=0.8715 | precision=0.9025 | recall=0.8576 | best_f1=0.8715

[Epoch 002/023] stage=warmup | lr_backbone=9.81459e-05 | lr_head=0.000490729
  train: loss=0.4131 | acc=0.8629 | f1=0.8364 | precision=0.8419 | recall=0.8318
  val:   loss=0.2507 | acc=0.9170 | f1=0.9034 | precision=0.9071 | recall=0.9023 | best_f1=0.9034

[Epoch 003/023] stage=warmup | lr_backbone=9.58606e-05 | lr_head=0.000479303
  train: loss=0.3592 | acc=0.8791 | f1=0.8567 | precision=0.8614 | recall=0.8528
  val:   loss=0.2273 | acc=0.9252 | f1=0.9133 | precision=0.9120 | recall=0.9172 | best_f1=0.9133

[Epoch 004/023] stage=warmup | lr_backbone=9.2721e-05 | lr_head=0.000463605
  train: loss=0.3183 | acc=0.8932 | f1=0.8744 | precision=0.8779 | recall=0.8714
  val:   loss=0.2055 | acc=0.9275 | f1=0.9144 | precision=0.9189 | recall=0.9107 | best_f1=0.9144

[Epoch 005/023] stage=warmup | lr_backbone=8.87856e-05 | lr_head=0.000443928
  train: loss=0.2862 | acc=0.9037 | f1=0.8890 | precision=0.8925 | recall=0.8860
  val:   loss=0.1937 | acc=0.9348 | f1=0.9237 | precision=0.9247 | recall=0.9239 | best_f1=0.9237

[Epoch 006/023] stage=warmup | lr_backbone=8.41277e-05 | lr_head=0.000420638
  train: loss=0.2657 | acc=0.9077 | f1=0.8930 | precision=0.8954 | recall=0.8910
  val:   loss=0.1797 | acc=0.9362 | f1=0.9262 | precision=0.9269 | recall=0.9258 | best_f1=0.9262

[Epoch 007/023] stage=warmup | lr_backbone=7.8834e-05 | lr_head=0.00039417
  train: loss=0.2450 | acc=0.9200 | f1=0.9079 | precision=0.9103 | recall=0.9058
  val:   loss=0.1764 | acc=0.9403 | f1=0.9299 | precision=0.9280 | recall=0.9331 | best_f1=0.9299

[Epoch 008/023] stage=warmup | lr_backbone=7.30033e-05 | lr_head=0.000365016
  train: loss=0.2318 | acc=0.9210 | f1=0.9086 | precision=0.9104 | recall=0.9072
  val:   loss=0.1767 | acc=0.9430 | f1=0.9348 | precision=0.9312 | recall=0.9391 | best_f1=0.9348

Switching to stage 2 fine-tuning: backbone unfrozen.

[Epoch 009/023] stage=finetune | lr_backbone=6.6744e-05 | lr_head=0.00033372
  train: loss=0.2684 | acc=0.9103 | f1=0.8983 | precision=0.8996 | recall=0.8975
  val:   loss=0.1642 | acc=0.9444 | f1=0.9366 | precision=0.9360 | recall=0.9402 | best_f1=0.9366


--- Training 2 ---
Training setup
  Device: cuda
  GPU: NVIDIA GeForce RTX 3050 Laptop GPU
  Model: resnet18_local_global
  Num classes: 7
  Train samples: 8778
  Val samples: 1621
  Train batches: 1098
  Val batches: 203
  Batch size: 8
  Epochs: stage1=8, stage2=15
  Optimizer: AdamW | backbone_lr=0.0001 | head_lr=0.0005 | wd=0.0001
  Scheduler: cosine | t_max=33
  CPU threads: intra_op=2 | interop=1
  Throttle per batch: 0.0 sec

[Epoch 001/023] stage=warmup | lr_backbone=9.95343e-05 | lr_head=0.000497671
  train: loss=0.6941 | acc=0.7609 | f1=0.7039 | precision=0.7213 | recall=0.6925
  val:   loss=0.3086 | acc=0.8914 | f1=0.8299 | precision=0.8357 | recall=0.8345 | best_f1=0.8299

[Epoch 002/023] stage=warmup | lr_backbone=9.81459e-05 | lr_head=0.000490729
  train: loss=0.4131 | acc=0.8622 | f1=0.8346 | precision=0.8399 | recall=0.8300
  val:   loss=0.3360 | acc=0.8840 | f1=0.8391 | precision=0.8436 | recall=0.8626 | best_f1=0.8391
....
[Epoch 007/023] stage=warmup | lr_backbone=7.8834e-05 | lr_head=0.00039417
  train: loss=0.2382 | acc=0.9154 | f1=0.9015 | precision=0.9021 | recall=0.9010
  val:   loss=0.2520 | acc=0.9173 | f1=0.8674 | precision=0.8585 | recall=0.8907 | best_f1=0.8863

[Epoch 008/023] stage=warmup | lr_backbone=7.30033e-05 | lr_head=0.000365016
  train: loss=0.2229 | acc=0.9229 | f1=0.9105 | precision=0.9127 | recall=0.9086
  val:   loss=0.2437 | acc=0.9297 | f1=0.8907 | precision=0.8798 | recall=0.9067 | best_f1=0.8907

Switching to stage 2 fine-tuning: backbone unfrozen.

[Epoch 009/023] stage=finetune | lr_backbone=6.6744e-05 | lr_head=0.00033372
  train: loss=0.2701 | acc=0.9125 | f1=0.8994 | precision=0.9002 | recall=0.8989
  val:   loss=0.2229 | acc=0.9371 | f1=0.9179 | precision=0.9277 | recall=0.9162 | best_f1=0.9179

[Epoch 010/023] stage=finetune | lr_backbone=6.01728e-05 | lr_head=0.000300864
  train: loss=0.1555 | acc=0.9504 | f1=0.9465 | precision=0.9470 | recall=0.9461
  val:   loss=0.2217 | acc=0.9432 | f1=0.9195 | precision=0.9303 | recall=0.9151 | best_f1=0.9195
....
[Epoch 023/023] stage=finetune | lr_backbone=0 | lr_head=0
  train: loss=0.0085 | acc=0.9962 | f1=0.9970 | precision=0.9971 | recall=0.9968
  val:   loss=0.2718 | acc=0.9630 | f1=0.9508 | precision=0.9627 | recall=0.9402 | best_f1=0.9544

Findings
This run is not “bad,” but it does start to overfit in late fine-tuning. The clearest sign is that after about epoch 11 to 14, train loss keeps dropping hard while validation loss stops improving and trends upward, even though validation F1/accuracy stay fairly high. In history.csv, val loss is best around epoch 11 (0.1807), while train loss keeps falling toward near-zero by the end.
Your code saves the “best” checkpoint by macro F1 only, not by validation loss. That means best.pt is chosen by the highest val_f1_macro, even if val loss is already getting worse. This is in train.py. So if you were watching loss, the checkpoint policy and your intuition were measuring different things.
A persistent train/val loss gap is expected in your current setup because training sees augmented data while validation is original-only. That is actually the correct split behavior now. The grouped split report shows no source leakage and original-only validation in train_val_split_report.md. So “validation loss higher than training loss” by itself is not the bug.
There is no early stopping. The loop always runs all epochs, even after validation loss has already started to worsen. That is in train.py. So the code currently allows mild late overfitting to continue unchecked.

Why It Happened
Warmup looked healthy. By epoch 8, both train and validation were improving.
Fine-tuning improved the model further, and the best validation F1 was around epochs 17 to 20.
After that, the model kept getting more confident on the training set, which drove train loss down, but that extra confidence did not translate into better generalization. Cross-entropy loss is especially sensitive to overconfident mistakes, so val loss can rise even while val accuracy/F1 stay similar.
So the pattern is:

not catastrophic failure
not the earlier leakage problem
mild overfitting plus a checkpoint criterion mismatch 

--> FIX WORK:
1- train transforms are too bare: only toTensor + normalization; need to add more augmentation to make training harder and reduce overfitting
2- training keeps running through all epochs with no early stopping
3- best model weights are chosen by macro F1, not by validation loss; need to change checkpoint policy to save best.pt by lowest val loss, not highest val F1
4- no evaluation, confusion matrix and inference


--- Training 3 ---
**Validation Split**

Yes, that validation distribution is reasonable.

Your split script is doing a grouped, class-wise split by source path, not a balanced validation set. It takes about `20%` of each class’s original source groups into validation in [create_grouped_train_val_split.py:51](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/scripts/splits/create_grouped_train_val_split.py#L51) and [create_grouped_train_val_split.py:72](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/scripts/splits/create_grouped_train_val_split.py#L72). That is why the report shows:
- `Naskh: 805`
- `Thuluth: 382`
- much fewer for the others

Those numbers are basically the original class imbalance reflected into validation:
- `Naskh`: `4025 -> 805`
- `Thuluth`: `1911 -> 382`
- `Diwani`: `657 -> 131`
- `Kufic`: `292 -> 58`
- `Muhaqaq`: `207 -> 41`

That makes sense if you want honest evaluation on the data as it really exists. It is not balanced, but it is realistic. The tradeoff is that minority-class metrics are noisier because `Kufic` has only `58` validation samples and `Muhaqaq` only `41` in [train_val_split_report.md](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/data/metadata/train_val_split_report.md). That is exactly why macro F1 matters more than accuracy here.

**What Fine-Tuning Actually Does**

Fine-tuning is not a new branch in the architecture. It is a change in which weights are allowed to update.

In your model, the trainable parts are:
- backbone: ResNet18 in [local_global_model.py:24](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/models/local_global_model.py#L24)
- local branch head in [local_global_model.py:25](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/models/local_global_model.py#L25)
- global pooling branch in [local_global_model.py:29](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/models/local_global_model.py#L29)
- fusion classifier in [local_global_model.py:30](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/models/local_global_model.py#L30)

During warmup, the backbone is frozen in [train.py:562](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/training/train.py#L562). So only the new heads learn.

When fine-tuning starts, the backbone is unfrozen in [train.py:615](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/training/train.py#L615). That means the ResNet stem and layers now also adapt to your Arabic calligraphy data with a smaller learning rate. So stage 2 is: same architecture, more parameters become trainable.

**History Analysis**

The important point is this: fine-tuning is not broken. It helps first, then it starts overfitting.

What the log says:
- Warmup was healthy. Train loss went from `0.7211` to `0.2363`, and val loss went from `0.3342` to `0.2547` by epoch 8 in [history.csv:2](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/outputs/training/resnet18_local_global_grouped_split/logs/history.csv#L2) and [history.csv:9](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/outputs/training/resnet18_local_global_grouped_split/logs/history.csv#L9).
- Right after unfreezing, fine-tuning improved validation clearly. At epoch 9, val F1 jumped to `0.9067`, and at epoch 10 val loss reached its best value `0.2056` in [history.csv:10](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/outputs/training/resnet18_local_global_grouped_split/logs/history.csv#L10) and [history.csv:11](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/outputs/training/resnet18_local_global_grouped_split/logs/history.csv#L11).
- Later, train loss kept collapsing toward zero, while val loss stopped improving and mostly drifted upward. By epoch 22, train loss was `0.0094` while val loss was `0.2332` in [history.csv:23](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/outputs/training/resnet18_local_global_grouped_split/logs/history.csv#L23).

So the pattern is:
- epochs `1-10`: good learning
- epochs `10-12`: still useful fine-tuning
- after that: stronger fit to training data than to validation

But there is one nuance that matters a lot:
- val loss got worse after epoch 10
- val macro F1 kept improving until epoch 22

That means later fine-tuning improved classification decisions, but worsened calibration. Cross-entropy loss punishes a few overconfident mistakes heavily. So the model can classify more samples correctly overall while still having higher validation loss.

That is exactly what happened here.

**What I Think**

The behavior is not “something is wrong with fine-tuning.” The more accurate statement is:

- the beginning of fine-tuning is useful and necessary
- the late part of fine-tuning is over-specializing the backbone
- the model becomes better at class separation, but more overconfident on the remaining hard mistakes

That also matches your evaluation:
- `best.pt` gives strong macro F1 around `0.9523`
- `best_loss.pt` is worse overall, about `0.9186`

So for this project:
- selecting by macro F1 makes sense, because the dataset is imbalanced
- selecting by loss would favor a more conservative but weaker classifier

**About The Figures**

The figures are showing a real phenomenon, not a plotting mistake:
- validation distribution is imbalanced, and that is expected
- fine-tuning helps first
- late fine-tuning causes mild overfitting and confidence inflation
- the hardest confusion region is around `Thuluth`, `Diwani`, and `Kufic`

If you want, next I can help you turn this exact interpretation into report-ready wording for the professor.
------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
**What’s happening**

This is real late-stage overfitting during fine-tuning, but with one important nuance: it is overfitting in **loss/calibration** more than in **macro-F1**.

From your history:
- best `val_loss` happens very early in fine-tuning: epoch 10, about `0.2056`
- best `val_f1_macro` happens much later: epoch 22, about `0.9523`
- meanwhile `train_loss` collapses from `0.2517` at epoch 9 to `0.0094` at epoch 22

So the backbone is learning more and more training-specific confidence after unfreezing. It still improves classification boundaries a bit, but it becomes too confident on the remaining validation mistakes. That is why F1 can still rise while validation loss stops improving.

This also explains why waiting to epoch 23 feels wasteful to you. If your goal is a stable model, not the absolute highest late-epoch F1, then your current fine-tuning policy is too long and too aggressive.

**Why this is happening in your current setup**

The biggest causes are:

- You unfreeze the **entire backbone at once** in [train.py:615](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/training/train.py#L615). That means stem, early layers, mid layers, and deep layers all start adapting to your dataset immediately.
- The backbone learning rate is still relatively strong for full unfreezing on this dataset: `1e-4` in [resnet18_local_global.yaml](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/configs/resnet18_local_global.yaml).
- Your train set is not huge in terms of unique real sources. The split report shows `6491` unique training sources, but `8778` training rows, meaning many are offline augmented siblings, not new real samples, in [train_val_split_report.md](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/data/metadata/train_val_split_report.md).
- Train-time transforms are currently almost empty in [transforms.py:7](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/datasets/transforms.py#L7). So once fine-tuning starts, the model keeps seeing the same fixed offline-augmented images and can memorize them.
- If you are using `weighted_sampler`, that can amplify memorization further by repeatedly drawing the same minority-class sources.

So the issue is not that fine-tuning is conceptually wrong. The issue is that your current fine-tuning setup is too broad and too unconstrained.

**What fine-tuning is actually doing here**

Architecturally, nothing new is added during fine-tuning. The model still has:
- backbone in [local_global_model.py:24](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/models/local_global_model.py#L24)
- local head in [local_global_model.py:25](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/models/local_global_model.py#L25)
- global head in [local_global_model.py:29](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/models/local_global_model.py#L29)
- fusion classifier in [local_global_model.py:30](/abs/path/c:/Users/user/OneDrive%20-%20American%20University%20of%20Beirut/Courses/NeuralNetworks-EECE693/Project/ArabicCalligraphyClassificationAndRecognition/src/models/local_global_model.py#L30)

Warmup:
- only the new heads learn

Fine-tuning:
- the pretrained ResNet layers also start changing

So the “fine-tuning section” is not a separate branch. It is the same model, with many more parameters now allowed to move.

**Best solutions to try, in order**

1. Shorten stage 2 aggressively.
- Your logs suggest the useful part of fine-tuning happens very early.
- A realistic stage-2 budget is probably `3 to 6` epochs, not `15`.

2. Lower the backbone LR a lot when unfreezing.
- `1e-4` for full-backbone unfreezing is likely too high here.
- The most likely stable range is closer to `1e-5` or `2e-5`.

3. Do not unfreeze the whole backbone at once.
- Unfreeze only `layer4` first.
- If needed later, unfreeze `layer3`.
- Keep stem, `layer1`, and `layer2` frozen.

4. Freeze BatchNorm behavior during fine-tuning.
- With batch size `8`, updating all BN statistics in the backbone can be noisy and can hurt stability.

5. Add mild online augmentation during training.
- Right now the model mostly sees a fixed offline augmented dataset.
- Fresh, mild online variation usually stabilizes fine-tuning.

6. Reconsider imbalance handling.
- If you are using a weighted sampler, it may be making repeated-source memorization worse.
- Class-weighted loss is often more stable than aggressive resampling in this kind of setup.

7. Add light regularization in stage 2 only.
- label smoothing around `0.05`
- slightly higher dropout
- possibly a bit more weight decay

**What I would do next**

If the goal is “stable training and stop early,” the best next protocol is:

- keep warmup mostly as is
- fine-tune only the upper backbone, not the whole ResNet
- reduce backbone LR sharply
- monitor `val_loss` for stopping
- still save `best_f1` separately for reporting

That would likely stop the run much earlier and make the curves look more sane.

**Important clarification**

If your main report metric is macro-F1, then the later fine-tuning epochs are not literally useless, because they did improve macro-F1. But if your goal is a stable, well-behaved model with less overfitting and shorter training, then yes: your current stage-2 setup is more aggressive than it should be.

If you want, next I can help you choose the exact fine-tuning policy before touching code:
- `shorter stage2`
- `smaller backbone LR`
- `partial unfreezing`
- `monitor choice: val_loss vs val_f1`


------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
What changed: FINETUNE FIX1

partial fine-tuning controls were added in backbone.py (line 53) and local_global_model.py (line 55)
backbone BatchNorm freezing was added in backbone.py (line 71) and applied during training in local_global_model.py (line 63)
light online train augmentation was added in transforms.py (line 8)
stage-2 LR switching was added in optimizer.py and used in train.py (line 639)
the training loop now uses a configurable fine-tuning policy in train.py (line 482) and switches stage 2 behavior in train.py (line 650)
New default policy in resnet18_local_global.yaml:

shorter stage 2: 8 epochs
early stopping on val_loss
partial unfreezing: layer3_layer4
freeze backbone BN stats
stage-2 LRs reduced to 2e-5 backbone and 2e-4 head
label smoothing 0.05
imbalance handling changed to class_weights with gentler power: 0.5
online augmentation enabled
new output root: outputs/training/resnet18_local_global_stable_finetune
Old behavior is still possible:

set fine_tuning.unfreeze_mode: full
set imbalance.strategy: weighted_sampler
disable online augmentation
change early stopping monitor back to val_f1_macro
I did not run a full training job. I syntax-checked the modified files and verified the training entry point loads correctly in your pytorch_course environment.

------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------
FINETUNE FIX 2 
What changed:

train.py
training loss still uses class-weighted + label-smoothed cross-entropy
validation loss is now plain cross-entropy for cleaner monitoring
scheduler now gets eta_min
scheduler.py
added eta_min support
new config: resnet18_local_global_lowval.yaml
The new config does this:

keeps warmup at 8 epochs
reduces fine-tuning to max 6 epochs
early stopping on val_loss
only unfreezes layer4
freezes backbone BatchNorm stats
lowers stage-2 LR to:
backbone 1e-5
head 1e-4
uses gentler class weighting: power = 0.35
keeps mild online augmentation
writes to a new folder:
outputs/training/resnet18_local_global_lowval
Old code and old configs are still there. I did not remove the previous training path.
In train.py, training uses class-weighted + label-smoothed cross-entropy, while validation uses plain cross-entropy.
About val_loss being lower than train_loss from the start:
Train images now get online augmentation in transforms.py.
Validation is clean and deterministic.